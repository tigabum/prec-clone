// import { createStyles, makeStyles } from "@mui/styles";
import { createStyles, makeStyles } from "@mui/styles";

export default makeStyles((theme) =>
  createStyles({
    root: {
      display: "flex",
      flexDirection: "column",
      height: "100vh",
      paddingTop: "75px",
    },
  })
);
